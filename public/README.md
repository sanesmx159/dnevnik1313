# Как это работает (публичная папка)
Статические файлы сайта находятся в папке public/. Сервер отдаёт их и предоставляет два API:
- GET /api/grades — вернуть текущие оценки (содержимое data.json)
- POST /api/grades — обновить оценки. Тело запроса: { password: "...", record: {...} }
